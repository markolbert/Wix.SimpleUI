<Window x:Class="Olbert.Wix.views.WixWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Olbert.Wix.ViewModels"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="300" Width="450.667">

    <Grid Background="Gainsboro">
        <Grid.DataContext>
            <DataTemplate DataType="{x:Type vm:WixViewModel}"></DataTemplate>
        </Grid.DataContext>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Image Grid.Row="0" Grid.Column="0" 
               Source="/assets/j4jtransparent.png"/>
        
        <ContentControl Grid.Row="0" Grid.Column="1"
                        Content="{Binding CurrentPanel}"/>
        
        <ContentControl Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                        Content="{Binding CurrentButtons}"/>
    </Grid>
</Window>
